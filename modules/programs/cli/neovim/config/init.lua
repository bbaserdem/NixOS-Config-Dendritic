--[[
-- NeoVim config stub entry
--]]

-- First line here would have been loading nix helpers
--require("nixCatsUtils").setup({
--  non_nix_value = true,
--})

-- Then we would move onto the main entry point for the configuration
--require("luaConf")
